use std::thread;
use std::sync::mpsc;
/*
pub struct Timer {
    duration: usize,
    control_channel: mpsc::SyncSender(Option<()>),
}

impl Timer {
    pub fn new(duration_ms: usize, out_channel: mpsc::SyncSender<()>) -> Timer {
        Timer{
            duration: duration_ms,
            guard: thread::(|duration| {

            })
        }
    }
}
*/
